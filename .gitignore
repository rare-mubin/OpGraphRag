# Ollama model weights (large binaries, ~10GB+, re-downloadable via `ollama pull`)
kg/ollama_models/

# Python
__pycache__/
*.pyc
*.pyo

# OS
.DS_Store
Thumbs.db

# Derived analysis cache -- regenerated in ~60s by kg/ablate_features.py
kg/output/ablation_features.npz

# HotpotQA source data (~97MB) -- download + prepare per README "Getting the dataset"
Dataset/
